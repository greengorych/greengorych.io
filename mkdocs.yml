# Site name
site_name: GreenGorych

# Site URL
site_url: https://greengorych.io

# Copyright setting
copyright: © 2025 Sergei Tilipin. Texts licensed under <a href="https://creativecommons.org/licenses/by-sa/4.0/" target="_blank">CC BY-SA 4.0</a>, code examples under <a href="https://opensource.org/licenses/MIT" target="_blank">MIT</a>.

# Theme settings
theme:
  # Theme name
  name: material
  # Color palette settings
  palette:
    # Palette toggle for automatic mode
    - media: "(prefers-color-scheme)"
      scheme: default
      toggle:
        icon: material/brightness-auto
        name: Switch to light mode
    # Palette toggle for light mode
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    # Palette toggle for dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: material/brightness-4
        name: Switch to system preference

# Plugins settings
plugins:
  # Blog plugin settings
  - blog:
      # Enable blog plugin
      enabled: true
      # Directory with blog posts
      blog_dir: .
      # URL pattern for blog posts
      post_url_format: "blog/{slug}"
      # Show blog post read time
      post_readtime: true
      # Enable blog archive
      archive: true
